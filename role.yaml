apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: team3-ns
  name: sa-role
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list", "watch", "create", "delete"]